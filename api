 import axios from 'axios';

const api = axios.create({
  // Em produção, lerá VITE_API_URL do Render. Localmente, usa o localhost.
  baseURL: import.meta.env.VITE_API_URL || 'http://localhost:10000'
});

export default api;