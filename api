 import axios from 'axios';

const api = axios.create({
  // O Vite carrega automaticamente variáveis que começam com VITE_
  baseURL:import.meta.env.VITE_API_URL || 'https://projeto-2-1.onrender.com',
});

export default api;